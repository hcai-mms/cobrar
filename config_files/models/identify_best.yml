experiment:
#    dataset: baby
    dataset: movielens_1m
    evaluation:
        cutoffs: [ 5 ]
        simple_metrics: [ nDCG, EFD, EPC, ARP, PopREO, PopRSP, ItemCoverage ]
        # EFD: novelty
        # EPC: novelty
        # ARP: average recommendation popularity
        # PopREO: Ranking-based equal-opportunity. Lower better?
        # ItemCoverage: Coverage
        paired_ttest: True
        wilcoxon_test: False
    models:
        #ProxyRecommender:
        #    path: ./results/emma/recs/FeatureItemKNN_nn=20_sim=cosine_msf=0$15_bin=False_modalites='emotion'_aggregation=concat_loads='EmotionAttribute'.tsv
        RecommendationFolder:
            folder: ./results/movielens_1m/recs_best/